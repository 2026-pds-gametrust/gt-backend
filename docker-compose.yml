services:
  localstack:
    image: localstack/localstack:4
    ports:
      - '4566:4566'
    environment:
      - SERVICES=sqs,sns
      - DEBUG=0
    volumes:
      - localstack_data:/var/lib/localstack

volumes:
  localstack_data:
